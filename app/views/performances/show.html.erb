<%= render "shared/header" %>


<%# <%= image_tag @performance.image %>

<%= @performance.title %>
<%= @performance.group %>
<%= @performance.price1 %>
<%= @performance.price2 %>
<%= @performance.price3 %>
<%= @performance.price4 %>
<%= @performance.price5 %>
    <table class="detail-table">
      <tbody>
        <tr>
          <th class="detail-performance">ジャンル</th>
          <td class="detail-value"><%= @performance.genre.name %></td>
        </tr>
        <tr>
          <th class="detail-performance">エリア(東京都)</th>
          <td class="detail-value"><%= @performance.area.name %></td>
        </tr>
        <tr>
          <th class="detail-performance">開演時間詳細</th>
          <td class="detail-value"><%= @performance.start_at%></td>
        </tr>
        <tr>
          <th class="detail-performance">チケット詳細</th>
          <td class="detail-value"><%= @performance.url %></td>
        </tr>
      </tbody>
    </table>



  <% if user_signed_in? && current_user.id == @performance.user.id %>
    <%= link_to '情報の編集', edit_performance_path, method: :get, class:"performance-red-btn" %>
    <p class='or-text'>or</p>
    <%= link_to '削除', performance_path(@performance.id), method: :delete, class:'item-destroy' %>
  <% end %>

<%= link_to 'マイページにもどる', "/users/#{current_user.id}" %>
  <%= link_to 'トップページにもどる', root_path %>


<%= render "shared/footer" %>